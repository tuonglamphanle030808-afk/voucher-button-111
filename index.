<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Voucher</title>
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: #f0f4ff;
      margin: 0;
    }
    .voucher {
      background: linear-gradient(135deg, #001f54, #4da8ff);
      padding: 40px 80px;
      border-radius: 20px;
      font-size: 50px;
      color: white;
      font-family: Arial;
      box-shadow: 0 0 40px rgba(0,0,0,0.3);
      animation: zoom 1.8s infinite ease-in-out;
    }
    @keyframes zoom {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.06); }
    }
  </style>
</head>
<body>
  <div class="voucher">VOUCHER</div>
</body>
</html>
